import React from 'react'
import Array from '../proarry'
const Project = ({prodouctsData}) => {
  return (
    <div className=' h-[100vh] flex flex-wrap '>Project
    
   {Array.map((item=>( <Array key={it.id}  Array={item}/>
 
    

    
    
 

   )))}
    
    </div>
  )
}

export default Project